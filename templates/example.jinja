<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8">
		<title>Kotlin by Example: Hello World</title>
		<link rel=stylesheet href="site.css">
	</head>
	<body>
		<div class="example">
			<h2><a href="./">Kotlin by Example</a>: Hello World</h2>

			<table>
				{% for row in metadata.code %}
				<tr>
					<td class="docs">
						<p>{{row[0]}}</p>
					</td>
					{% if row[1]|length > 0 %}
					<td class="code">
						<pre><code>{% for line in row[1] %}<span class="line"><span class="cl">{{line}}</span></span>
{% endfor %}</code></pre>
					{% else %}
					<td class="code empty"></td>
					{% endif %}
					</td>
				</tr>
				{% endfor %}
			</table>

			<table>
				{% for row in metadata.script %}
				<tr>
					<td class="docs">
						<p>{{row[0]}}</p>
					</td>
					<td class="code">
						<pre><code>{% for line in row[1] %}<span class="line"><span class="cl">{{line}}</span></span>
{% endfor %}</code></pre>
					</td>
				</tr>
				{% endfor %}
			</table>

			<p class="footer">by <a href="https://rubenvannieuwpoort.nl">Ruben van Nieuwpoort</a>, based on <a href="https://gobyexample.com">Go by Example</a> by <a href="https://markmcgranaghan.com">Mark McGranaghan</a> and <a href="https://eli.thegreenplace.net">Eli Bendersky</a> | TODO(Ruben): add link to source and license</p>
		</div>
	</body>
</html>
