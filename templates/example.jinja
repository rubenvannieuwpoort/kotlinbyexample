<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8">
		<title>Kotlin by Example: {{title}}</title>
		<link rel=stylesheet href="site.css">
	</head>
	<body>
		<div class="example">
			<h2><a href="/">Kotlin by Example</a>: {{title}}</h2>

			<table>
				{% for row in code %}
				<tr>
					<td class="docs">
						{{row[0]}}
					</td>
					{% if row[1]|length == 0 and (loop.index == 1 or loop.index == code | length) %}
					<td class="code empty"></td>
					{% else %}
					<td class="code">
						{% if (loop.index0 == first_code_idx) %}
						<a href="https://play.kotlinlang.org/#{{base64}}"><img title="Run code" src="play.png" class="run"></a>
						<img title="Copy code" src="clipboard.png" class="copy" onclick="navigator.clipboard.writeText(code);">
						{% endif %}
						<pre><code>{% for line in row[1] %}<span class="line"><span class="cl">{{line}}</span></span>
{% endfor %}</code></pre>
					{% endif %}
					</td>
				</tr>
				{% endfor %}
			</table>

			<table>
				{% for row in script %}
				<tr>
					<td class="docs">
						{{row[0]}}
					</td>
					<td class="code">
						<pre><code>{% for line in row[1] %}<span class="line"><span class="cl">{{line}}</span></span>
{% endfor %}</code></pre>
					</td>
				</tr>
				{% endfor %}
			</table>

			<p class="footer">by <a href="https://rubenvannieuwpoort.nl">Ruben van Nieuwpoort</a> | <a href="https://github.com/rubenvannieuwpoort/kotlinbyexample/blob/master/src/examples/{{dirname}}/main.kt">source</a> | <a href="https://creativecommons.org/licenses/by/3.0/">license</a> | based on <a href="https://gobyexample.com">Go by Example</a></p>
		</div>
		<script>
			var code = {{sourcecode}};
		</script>
	</body>
</html>
